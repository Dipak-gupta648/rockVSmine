# rockVSmine
This notebook classifies sonar signals as rock or mine using Logistic Regression. It covers data loading, preprocessing, train-test split, model training, evaluation (accuracy on train/test data), and a predictive system for new inputs. It's a full ML classification pipeline from data to prediction.
